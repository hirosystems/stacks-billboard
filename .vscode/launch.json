{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "clarinet",
      "request": "launch",
      "name": "Call .billboard.get-message",
      "manifest": "${workspaceFolder}/Clarinet.toml",
      "expression": "(contract-call? .billboard get-message)"
    }
  ]
}
